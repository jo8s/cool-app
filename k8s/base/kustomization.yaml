apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Shared manifests only. Namespace, replicas, ingress host and the image tag
# are all set by the per-environment overlays in ../overlays/{dev,staging,prod}.
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
